trigger:
- main

pool:
  vmImage: 'windows-latest'

steps:
- task: CmdLine@2
  inputs:
    script: |
      echo YAML pipline
      dir=$(pwd)
      du -sh $dir

- task: WhiteSource@20
  inputs:
    cwd: 'npm/public'
    projectName: 'Dropwizard/npm/public'
    configuration: |
      checkPolicies=true
      productName=Salih2
